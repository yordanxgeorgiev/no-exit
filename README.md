# no-exit
A toy C server implementation for an escape room game.

## Getting started
Use `.env.example` as template to create `.env`, fill out the credentials and start
the server by:

```bash
docker compose up --build